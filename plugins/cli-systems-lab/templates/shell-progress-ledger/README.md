# __APP_TITLE__ Starter

This template is a shell progress-ledger starter for build, release, install, or migration flows.

## Run

```bash
bash ./run.sh
```

## Why it exists

- stable step labels
- inline warnings
- artifact summary at the end
- plain log-safe behavior by default
