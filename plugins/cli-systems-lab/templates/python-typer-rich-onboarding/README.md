# __APP_TITLE__ Starter

This template is a Typer plus Rich onboarding surface with a non-interactive fallback.

## Run

```bash
python app.py setup
```

## Notes

- `--plain` forces the non-interactive path
- `NO_COLOR=1` disables Rich styling
- prompts are skipped when stdin is not interactive
