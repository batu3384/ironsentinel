# __APP_TITLE__ Starter

This template is an oclif plus Ink dashboard starter with a plain fallback path.

## Run

```bash
npm install
node ./bin/run.js dashboard
```

## Fallback

- `NO_COLOR=1` disables the interactive Ink view
- non-interactive shells use a plain summary
- `q` exits the dashboard
