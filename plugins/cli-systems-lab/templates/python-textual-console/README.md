# __APP_TITLE__ Starter

This template is a Textual console starter with a plain fallback path.

## Run

```bash
python app.py console
```

## Notes

- `NO_COLOR=1` forces the plain fallback path
- non-interactive terminals print a plain status summary
- `q` exits the Textual screen
